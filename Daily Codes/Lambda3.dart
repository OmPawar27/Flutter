void main(){

    (int a, int b){
        print(a+b);
    }(10,20);   // These lambda functions can be called only ONCE in the code
                // Do not return any value in this type of function as they cannot be called
    print("End");
}